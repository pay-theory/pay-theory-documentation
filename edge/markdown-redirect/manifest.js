/**
 * Fallback manifest for local imports and safe Lambda startup.
 *
 * Deployment packaging replaces this file with the build-generated
 * `build/llm-docs/markdown-routes-manifest.json` contents.
 */
module.exports = {
  version: 1,
  routes: {},
};
